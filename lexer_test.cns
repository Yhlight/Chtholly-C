// This is a single line comment

/*
This is a
multi-line comment
*/

let x = 10;
let y = 20.5;
let z = "hello";
let a = 'c';

if (x > y) {
    print("x is greater than y");
} else {
    print("x is not greater than y");
}

func add(a: int, b: int) -> int {
    return a + b;
}

struct Point {
    x: int,
    y: int
}

let p = Point{x: 1, y: 2};
p.x = 3;

let arr = [1, 2, 3];
let val = arr[0];

let ref = &x;
let mut_ref = &mut x;

// Mismatched tokens for error checking
let unterminated_string = "hello
